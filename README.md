# AgentBeats Orchestrator

Thin orchestration repository for Purple runtime experiments on MCU benchmark tasks.

This repository focuses on:

- Purple runtime orchestration
- MCU benchmark execution
- Planner / actor / evaluator / memory / transition interfaces
- OpenHA actor adapter

This repository does not vendor large model implementations. It is designed to sit on top of existing model repositories and expose a clean runtime loop.
